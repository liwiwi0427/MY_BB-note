rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /baby_records/{syncCode} {
      allow read, write: if true;
    }
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
